"""The cost/quality benchmark: a mixed-workload harness comparing the built-in routing
strategies against an always-frontier baseline. Not imported by `src/` or `tests/` —
see `benchmark/README.md` for what it is and how to run it.
"""
